# Contributing

Bugs and probe ideas are very welcome — open an issue first if it's a big change.

## Setup

```bash
pip install -e ".[dev]"
pytest -q
```

## Adding a probe

See [docs/adding_probes.md](docs/adding_probes.md).

## Style

Nothing strict. `ruff` is run in CI but isn't a hard gate.
