# mm-probe-suite

Probing tasks for multimodal LLMs. WIP.

## Why

I keep finding that MLLM scores on VQA tasks don't really tell me whether the
model "sees" what I think it sees. This is a sandbox for small, targeted probes
that try to isolate specific capabilities.

## Status

Very early. Things will move around.
